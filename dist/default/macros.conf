[wg_epdr_default_index]
definition = (index=endpoint)
description = Update this macro to match the configured EDPR index

[wg_firebox_default_index]
definition = (index=netfw)
description = Update this macro to match the configured Firebox index